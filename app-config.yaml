app:
  title: Backstage
  baseUrl: http://localhost:3000

  # Enable all packages by default, this will discover packages from packages/app/package.json
  packages: all

  extensions:
    # Disable the nav items that we're manually rendering in packages/app/src/modules/nav/Sidebar.tsx
    - nav-item:search: false
    - nav-item:user-settings: false
    - nav-item:catalog: false
    - nav-item:scaffolder: false

    # Configure the catalog index page to be the root page, this is normally mounted on /catalog
    - page:catalog:
        config:
          path: /

    - entity-card:tech-insights/scorecards:
        config:
          filter: { kind: component }
    - entity-content:tech-insights/scorecards-content: false

argocd:
  fullDeploymentHistory: true
  appLocatorMethods:
    - type: 'config'
      instances:
        - name: argo
          url: https://argo.shifman.dev
          token: ${ARGOCD_KEY}

organization:
  name: Shifman

backend:
  # Used for enabling authentication, secret is shared by all backend plugins
  # See https://backstage.io/docs/auth/service-to-service-auth for
  # information on the format
  # auth:
  #   keys:
  #     - secret: ${BACKEND_SECRET}
  baseUrl: http://localhost:7007
  listen:
    port: 7007
    # Uncomment the following host directive to bind to specific interfaces
    # host: 127.0.0.1
  csp:
    connect-src: ["'self'", 'http:', 'https:']
    # Content-Security-Policy directives follow the Helmet format: https://helmetjs.github.io/#reference
    # Default Helmet Content-Security-Policy values can be removed by setting the key to false
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  # This is for local development only, it is not recommended to use this in production
  # The production database configuration is stored in app-config.production.yaml
  database:
    client: better-sqlite3
    connection: ':memory:'
  # see https://backstage.io/docs/ai/mcp-actions#actions-configuration for more details
  actions:
    pluginSources:
      - auth
      - catalog
      - scaffolder
  # workingDirectory: /tmp # Use this to configure a working directory for the scaffolder, defaults to the OS temp-dir

integrations:
  github:
    - host: github.com
      # This is a Personal Access Token or PAT from GitHub. You can find out how to generate this token, and more information
      # about setting up the GitHub integration here: https://backstage.io/docs/integrations/github/locations#configuration
      token: ${GITHUB_TOKEN}
    ### Example for how to add your GitHub Enterprise instance using the API:
    # - host: ghe.example.net
    #   apiBaseUrl: https://ghe.example.net/api/v3
    #   token: ${GHE_TOKEN}

proxy:
  ### Example for how to add a proxy endpoint for the frontend.
  ### A typical reason to do this is to handle HTTPS and CORS for internal services.
  # endpoints:
  #   '/test':
  #     target: 'https://example.com'
  #     changeOrigin: true

# Reference documentation http://backstage.io/docs/features/techdocs/configuration
# Note: After experimenting with basic setup, use CI/CD to generate docs
# and an external cloud storage when deploying TechDocs for production use-case.
# https://backstage.io/docs/features/techdocs/how-to-guides#how-to-migrate-from-techdocs-basic-to-recommended-deployment-approach
techdocs:
  builder: 'local' # Alternatives - 'external'
  generator:
    runIn: 'docker' # Alternatives - 'local'
  publisher:
    type: 'local' # Alternatives - 'googleGcs' or 'awsS3'. Read documentation for using alternatives.

auth:
  environment: development
  session:
    secret: localenv123
  # see https://backstage.io/docs/auth/ to learn about auth providers
  providers:
    oidc:
      development:
        metadataUrl: https://keycloak.shifman.dev/realms/master/.well-known/openid-configuration
        clientId: backstage
        clientSecret: ${BACKSTAGE_KEY}
        signIn:
          resolvers:
            - resolver: emailMatchingUserEntityProfileEmail
    # See https://backstage.io/docs/auth/guest/provider
    guest: {}
  # see https://backstage.io/docs/ai/mcp-actions#client-id-metadata-documents
  # to learn more about client id metadata documents
  experimentalClientIdMetadataDocuments:
    enabled: false
    # Optional: restrict which `client_id` URLs are allowed (defaults to ['*'])
    # allowedClientIdPatterns:
    #   - 'https://example.com/*'
    #   - 'https://*.trusted-domain.com/*'
    # Optional: restrict which redirect URIs are allowed (defaults to ['*'])
    # allowedRedirectUriPatterns:
    #   - 'http://localhost:*'
    #   - 'https://*.example.com/*'

scaffolder:
  # see https://backstage.io/docs/features/software-templates/configuration for software template options

catalog:
  providers:
    github:
      providerId:
        organization: 'aaronshifman' # string
        catalogPath: '**/catalog-info.yaml' # string
        filters:
          branch: 'main' # string
          repository: '.*' # Regex
        schedule: # same options as in SchedulerServiceTaskScheduleDefinition
          # supports cron, ISO duration, "human duration" as used in code
          frequency: { minutes: 30 }
          # supports ISO duration, "human duration" as used in code
          timeout: { minutes: 3 }
    k8SCnpgProvider:
      clusterUrl: ${KUBERNETES_CP}
      caData: ${CA_DATA}
      token: ${KUBERNETES_TOKEN}
      owner: group:default/platform
      schedule:
        frequency: { minutes: 10 }
        timeout: { minutes: 2 }
    keycloakOrg:
      default:
        baseUrl: https://keycloak.shifman.dev
        loginRealm: master
        realm: master
        clientId: backstage
        clientSecret: ${BACKSTAGE_KEY}
        schedule:
          frequency: { minutes: 30 }
          timeout: { minutes: 3 }
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, API, Resource, Location]
  locations:
    # Local example data, file locations are relative to the backend process, typically `packages/backend`
    - type: file
      target: ../../examples/entities.yaml

    # Local example template
    - type: file
      target: ../../examples/template/template.yaml
      rules:
        - allow: [Template]

    # Local example organizational data
    - type: file
      target: ../../examples/org.yaml
      rules:
        - allow: [User, Group]

    ## Uncomment these lines to add more example data
    # - type: url
    #   target: https://github.com/backstage/backstage/blob/master/packages/catalog-model/examples/all.yaml

    ## Uncomment these lines to add an example org
    # - type: url
    #   target: https://github.com/backstage/backstage/blob/master/packages/catalog-model/examples/acme-corp.yaml
    #   rules:
    #     - allow: [User, Group]

kubernetes:
  frontend:
    podDelete:
      enabled: true
  serviceLocatorMethod:
    type: singleTenant
  clusterLocatorMethods:
    - type: 'config'
      clusters:
        - url: ${KUBERNETES_CP}
          name: shifman
          authProvider: serviceAccount
          serviceAccountToken: ${KUBERNETES_TOKEN}
          caData: ${CA_DATA}

kubescape:
  clusterUrl: ${KUBERNETES_CP}
  caData: ${CA_DATA}
  token: ${KUBERNETES_TOKEN}

# see https://backstage.io/docs/permissions/getting-started for more on the permission framework
permission:
  # setting this to `false` will disable permissions
  enabled: true

# see https://backstage.io/docs/ai/mcp-actions for more details
mcpActions:
  name: 'My Company Backstage' # defaults to "backstage"
  description: 'Tools for managing your software catalog, creating new services from templates, and exploring your developer portal' # optional

techInsights:
  factRetrievers:
    componentCompletenessFactRetriever:
      cadence: '0 3 * * *' # daily at 03:00
      initialDelay: { seconds: 90 }
      lifecycle: { timeToLive: { weeks: 2 } }
  factChecker:
    checks:
      hasOwner:
        type: json-rules-engine
        name: Has owner
        description: Component has a non-unknown owner
        factIds: [componentCompletenessFactRetriever]
        rule:
          conditions:
            all:
              - fact: hasOwner
                operator: equal
                value: true
      hasSystem:
        type: json-rules-engine
        name: Belongs to a System
        description: Component declares spec.system
        factIds: [componentCompletenessFactRetriever]
        rule:
          conditions:
            all:
              - fact: hasSystem
                operator: equal
                value: true
      hasDescription:
        type: json-rules-engine
        name: Has description
        description: Component metadata has a description
        factIds: [componentCompletenessFactRetriever]
        rule:
          conditions:
            all:
              - fact: hasDescription
                operator: equal
                value: true
      hasSourceLocation:
        type: json-rules-engine
        name: Has source location
        description: Component has a backstage.io/source-location annotation
        factIds: [componentCompletenessFactRetriever]
        rule:
          conditions:
            all:
              - fact: hasSourceLocation
                operator: equal
                value: true
      hasTechdocs:
        type: json-rules-engine
        name: Has TechDocs
        description: Component has a backstage.io/techdocs-ref annotation
        factIds: [componentCompletenessFactRetriever]
        rule:
          conditions:
            all:
              - fact: hasTechdocs
                operator: equal
                value: true
      gitopsWired:
        type: json-rules-engine
        name: GitOps wired
        description: Component has both an ArgoCD app and a Kubernetes annotation
        factIds: [componentCompletenessFactRetriever]
        rule:
          conditions:
            all:
              - fact: gitopsWired
                operator: equal
                value: true
